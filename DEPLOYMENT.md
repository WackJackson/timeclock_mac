# 完整部署步骤

## 环境信息

- **云开发环境ID**：`timebomp-2g4siacf47b7e567`
- **小程序AppID**：`wx0a2f729bdf5d1b65`

---

## 一、云函数部署

### 1. login 云函数（用户登录注册）

#### 步骤：
1. 在微信开发者工具中，找到 `cloudfunctions/login` 文件夹
2. 右键点击 `login` 文件夹 → 选择 **"在终端中打开"**
3. 在终端中执行：
   ```bash
   npm install
   ```
4. 等待依赖安装完成
5. 右键点击 `login` 文件夹
6. 选择 **"上传并部署：云端安装依赖"**
7. 等待上传完成（状态栏会显示"上传成功"）

#### 验证：
- 打开云开发控制台 → 云函数
- 查看 `login` 函数是否显示"已部署"
- 查看函数详情，确认版本号已更新

---

### 2. syncData 云函数（数据同步）

#### 步骤：
1. 在微信开发者工具中，找到 `cloudfunctions/syncData` 文件夹
2. 右键点击 `syncData` 文件夹 → 选择 **"在终端中打开"**
3. 在终端中执行：
   ```bash
   npm install
   ```
4. 等待依赖安装完成
5. 右键点击 `syncData` 文件夹
6. 选择 **"上传并部署：云端安装依赖"**
7. 等待上传完成

#### 验证：
- 打开云开发控制台 → 云函数
- 查看 `syncData` 函数是否显示"已部署"
- 查看函数详情，确认版本号已更新

---

## 二、云数据库配置

### 1. 创建 users 集合（用户信息）

#### 步骤：
1. 打开 [微信云开发控制台](https://console.cloud.tencent.com/tcb)
2. 选择环境：`timebomp-2g4siacf47b7e567`
3. 点击左侧菜单 **"数据库"**
4. 点击 **"添加集合"** 按钮
5. 集合名称输入：`users`
6. 点击 **"确定"**

#### 权限设置：
1. 点击 `users` 集合
2. 点击 **"权限设置"** 标签
3. 选择权限模板：**"仅创建者可读写"**
4. 点击 **"保存"**

#### 字段说明：
| 字段名 | 类型 | 说明 |
|--------|------|------|
| _id | string | 自动生成 |
| _openid | string | 用户openid（自动添加） |
| nickName | string | 用户昵称 |
| avatarUrl | string | 用户头像URL |
| createTime | Date | 创建时间 |
| updateTime | Date | 更新时间 |

---

### 2. 创建 user_data 集合（用户业务数据）

#### 步骤：
1. 在云开发控制台的 **"数据库"** 页面
2. 点击 **"添加集合"** 按钮
3. 集合名称输入：`user_data`
4. 点击 **"确定"**

#### 权限设置：
1. 点击 `user_data` 集合
2. 点击 **"权限设置"** 标签
3. 选择权限模板：**"仅创建者可读写"**（重要！）
4. 点击 **"保存"**

#### 字段说明：
| 字段名 | 类型 | 说明 |
|--------|------|------|
| _id | string | 自动生成 |
| _openid | string | 用户openid（自动添加） |
| userConfig | Object | 用户配置（月薪、工作日、时段） |
| workRecords | Object | 工作记录 |
| wishes | Array | 愿望列表 |
| activeWish | number | 当前激活愿望ID |
| todayEarnings | Object | 今日收入 |
| todayEarningsHistory | Object | 收入历史 |
| firstWorkDate | string | 首次工作日期 |
| currentMode | string | 当前工作模式 |
| createTime | Date | 创建时间 |
| updateTime | Date | 更新时间 |
| lastSyncTime | Date | 最后同步时间 |

---

## 三、项目配置

### 1. 确认 project.config.json

确保文件中包含云函数根目录配置：
```json
{
  "cloudfunctionRoot": "cloudfunctions/"
}
```

### 2. 确认 app.js 云开发初始化

确保代码中包含：
```javascript
wx.cloud.init({
  env: 'timebomp-2g4siacf47b7e567',
  traceUser: true
});
```

---

## 四、测试验证

### 1. 测试登录功能

#### 步骤：
1. 在微信开发者工具中编译项目
2. 点击底部 tab 切换到 **"我的"** 页面
3. 点击 **"点击登录"** 按钮
4. 在弹出的授权窗口中点击 **"允许"**
5. 等待登录完成

#### 验证：
- 页面显示微信头像和昵称
- 控制台无报错信息
- 打开云开发控制台 → 数据库 → `users` 集合
- 查看是否有新增一条记录（包含你的昵称和头像）

---

### 2. 测试自动同步功能

#### 步骤：
1. 在小程序中设置工作配置（月薪、工作日、时段）
2. 添加一个愿望
3. 等待5-10秒

#### 验证：
- 打开云开发控制台 → 数据库 → `user_data` 集合
- 查看是否有新增一条记录
- 记录中是否包含你刚才设置的数据：
  - `userConfig` 中有月薪、工作日、时段
  - `wishes` 数组中有你添加的愿望
  - `lastSyncTime` 是最近的时间

---

### 3. 测试跨设备同步

#### 步骤：
1. 在微信开发者工具中添加一些数据（配置、愿望等）
2. 点击工具栏的 **"预览"** 按钮，生成二维码
3. 用真机微信扫码打开小程序
4. 登录相同的微信账号
5. 查看数据是否自动同步过来

#### 验证：
- 真机上能看到在开发工具中设置的所有数据
- 配置、愿望、收入等信息完全一致

---

## 五、常见问题排查

### 1. 云函数调用失败

**问题表现**：
- 登录时提示"登录失败"
- 控制台显示云函数调用错误

**排查步骤**：
1. 确认云函数已成功部署
   - 云开发控制台 → 云函数 → 查看状态
2. 查看云函数日志
   - 点击函数名 → 日志 → 查看错误信息
3. 确认环境ID正确
   - `app.js` 中的 `env` 是否为 `timebomp-2g4siacf47b7e567`

**解决方案**：
- 重新部署云函数
- 检查云函数代码是否有语法错误

---

### 2. 数据库权限错误

**问题表现**：
- 同步时提示"权限不足"
- 控制台显示 `errCode: -502003`

**排查步骤**：
1. 检查数据库权限设置
   - 云开发控制台 → 数据库 → 点击集合 → 权限设置
2. 确认权限为"仅创建者可读写"

**解决方案**：
- 将权限改为"仅创建者可读写"
- 保存后重试

---

### 3. 数据未同步

**问题表现**：
- 修改数据后，云端没有更新
- 换设备登录看不到数据

**排查步骤**：
1. 确认已登录
   - 查看"我的"页面是否显示头像和昵称
2. 查看控制台日志
   - 是否有"自动同步成功"的日志
3. 检查网络连接
   - 确保有网络访问权限

**解决方案**：
- 确保已登录状态
- 检查 `syncData` 云函数是否部署成功
- 手动触发同步：进入"我的"页面

---

### 4. 老用户登录显示"注册成功"

**问题表现**：
- 退出后重新登录，提示"注册成功"而非"登录成功"

**排查步骤**：
1. 查看云数据库 `users` 集合
2. 确认是否有对应的用户记录

**解决方案**：
- 重新部署 `login` 云函数（已修复此问题）
- 删除云数据库中的测试数据后重新登录

---

## 六、性能优化建议

### 1. 云函数冷启动优化
- 云函数会在一段时间不使用后进入冷启动
- 首次调用可能较慢（1-3秒）
- 正常情况：后续调用很快（100-300ms）

### 2. 数据库查询优化
- `users` 和 `user_data` 集合都有索引
- 查询通过 `_openid` 自动过滤，速度很快
- 单用户数据量小，查询响应快

### 3. 同步频率控制
- 已实现5秒节流，无需额外优化
- 定时同步为5分钟一次，可根据需要调整

---

## 七、备份与恢复

### 数据备份
1. 打开云开发控制台 → 数据库
2. 点击 `user_data` 集合
3. 点击 **"导出"** 按钮
4. 选择导出格式（JSON 或 CSV）
5. 下载备份文件

### 数据恢复
1. 打开云开发控制台 → 数据库
2. 点击 `user_data` 集合
3. 点击 **"导入"** 按钮
4. 选择之前导出的文件
5. 点击确认导入

---

## 八、部署检查清单

部署完成后，请检查以下项目：

- [ ] login 云函数已部署
- [ ] syncData 云函数已部署
- [ ] users 集合已创建，权限为"仅创建者可读写"
- [ ] user_data 集合已创建，权限为"仅创建者可读写"
- [ ] 登录功能正常（能获取用户信息）
- [ ] 自动同步功能正常（云数据库有数据）
- [ ] 跨设备数据同步正常（真机测试通过）
- [ ] 控制台无报错信息

---

## 九、后续维护

### 云函数更新
如需更新云函数代码：
1. 修改本地代码
2. 右键云函数文件夹
3. 选择"上传并部署：云端安装依赖"
4. 等待部署完成

### 数据库维护
定期检查：
- 数据库存储容量（免费版有限制）
- 数据完整性
- 必要时清理过期数据

### 监控建议
1. 定期查看云函数调用日志
2. 监控数据库读写次数
3. 关注用户反馈的同步问题

---

## 联系支持

如遇到无法解决的问题，请：
1. 查看控制台错误日志
2. 查看云函数运行日志
3. 截图错误信息
4. 联系开发者获取支持

---

**部署完成后，即可正常使用小程序的所有功能！**
