# 自动同步测试指南

## 测试前准备

### 1. 确认部署完成
- ✅ login 云函数已部署
- ✅ syncData 云函数已部署
- ✅ users 集合已创建
- ✅ user_data 集合已创建

### 2. 打开必要的窗口
- **微信开发者工具**（编译运行小程序）
- **云开发控制台**（查看数据库）
- **开发者工具控制台**（查看日志）

---

## 测试一：登录后自动同步

### 步骤：

1. **清空本地数据（可选）**
   - 在开发者工具中，点击菜单 → 清除缓存 → 清除数据缓存
   - 或者直接删除本地存储

2. **进行首次设置**
   - 编译运行小程序
   - 按照引导完成4步设置：
     - 月薪：输入 15000
     - 工作日：选择周一到周五
     - 工作时段：保持默认或自定义
     - 工龄：选择一个日期

3. **登录账号**
   - 切换到"我的"页面
   - 点击"点击登录"
   - 授权微信信息

4. **观察控制台日志**
   - 打开开发者工具底部的"Console"标签
   - 应该看到以下日志：
   ```
   新用户数据已上传到云端
   ```
   或
   ```
   数据同步成功
   自动同步成功
   ```

5. **验证云数据库**
   - 打开云开发控制台
   - 点击"数据库" → 选择 `user_data` 集合
   - 应该看到一条新记录，展开查看：
     - `userConfig` 中有你设置的月薪、工作日、时段
     - `firstWorkDate` 是你选择的工龄日期
     - `lastSyncTime` 是刚才的时间

### 预期结果：
✅ 控制台显示"新用户数据已上传到云端"
✅ user_data 集合中有完整的数据记录
✅ 没有报错信息

---

## 测试二：修改配置自动同步

### 步骤：

1. **修改月薪**
   - 在"我的"页面，点击"月薪设置"
   - 修改月薪为 20000
   - 点击"保存"

2. **观察控制台**
   - 5秒内应该看到日志：
   ```
   自动同步成功
   ```
   - 如果在5秒内多次修改，可能看到：
   ```
   同步节流中，跳过本次同步
   ```

3. **验证云数据库**
   - 刷新云数据库 user_data 集合
   - 点击你的记录查看详情
   - `userConfig.salary` 应该是 "20000"
   - `updateTime` 和 `lastSyncTime` 应该更新为最新时间

### 预期结果：
✅ 控制台显示"自动同步成功"
✅ 云端数据已更新为 20000
✅ 时间戳已更新

---

## 测试三：添加愿望自动同步

### 步骤：

1. **添加愿望**
   - 切换到"攒钱"页面
   - 点击右下角"+"按钮
   - 输入愿望名称：测试愿望
   - 输入目标金额：5000
   - 选择一个 emoji
   - 点击"创建"

2. **观察控制台**
   - 应该看到：
   ```
   自动同步成功
   ```

3. **验证云数据库**
   - 刷新 user_data 集合
   - 查看你的记录
   - `wishes` 数组中应该有你刚创建的愿望
   - 包含：name、emoji、targetAmount、currentAmount 等字段

### 预期结果：
✅ 控制台显示"自动同步成功"
✅ wishes 数组中有新愿望
✅ 愿望信息完整

---

## 测试四：定时自动同步（5分钟）

### 步骤：

1. **保持小程序运行**
   - 不做任何操作
   - 等待 5 分钟

2. **观察控制台**
   - 5分钟后应该看到：
   ```
   自动同步成功
   ```

3. **验证云数据库**
   - 查看 `lastSyncTime` 是否更新为当前时间

### 预期结果：
✅ 每5分钟自动同步一次
✅ lastSyncTime 定期更新

### 快速测试（可选）：
如果不想等5分钟，可以在 `app.js` 中临时修改同步间隔：
```javascript
// 改为30秒测试
this.globalData.syncTimer = setInterval(() => {
  this.triggerSync();
}, 30 * 1000); // 30秒
```

---

## 测试五：页面切换自动同步

### 步骤：

1. **修改一些数据**
   - 添加一个工作时段
   - 或修改工作日

2. **切换页面**
   - 从"时薪"页面切换到"我的"页面

3. **观察控制台**
   - 应该看到：
   ```
   自动同步成功
   ```

4. **验证云数据库**
   - 数据应该已经同步到云端

### 预期结果：
✅ 进入"我的"页面触发同步
✅ 数据已更新

---

## 测试六：跨设备同步（重要）

### 步骤：

1. **在开发工具中准备数据**
   - 设置好配置
   - 添加几个愿望
   - 记录一些工作时间
   - 确保已登录

2. **等待同步完成**
   - 查看控制台确认"自动同步成功"
   - 或查看云数据库确认数据已上传

3. **真机测试**
   - 点击开发者工具顶部的"预览"按钮
   - 用手机微信扫描二维码
   - 打开小程序
   - 使用相同的微信账号登录

4. **观察真机**
   - 登录后应该看到控制台日志：
   ```
   数据同步成功
   ```
   - 所有数据应该自动出现：
     - 配置信息（月薪、工作日、时段）
     - 愿望列表
     - 工作记录
     - 收入统计

5. **反向测试**
   - 在手机上修改月薪
   - 等待5-10秒
   - 在开发工具中刷新"我的"页面
   - 应该能看到手机上的修改

### 预期结果：
✅ 真机登录后自动下载云端数据
✅ 所有数据完整显示
✅ 双向同步正常

---

## 测试七：小程序前后台切换

### 步骤：

1. **修改数据**
   - 添加一个愿望

2. **切换到后台**
   - 点击开发者工具模拟器的"home"按钮
   - 或切换到其他页面

3. **观察控制台**
   - 应该看到：
   ```
   自动同步成功
   ```

4. **切换回前台**
   - 点击小程序图标重新打开
   - 应该再次触发同步

### 预期结果：
✅ 后台切换触发同步
✅ 前台切换触发同步

---

## 查看同步日志的方法

### 方法1：开发者工具控制台
```
打开开发者工具 → 底部 Console 标签 → 查看日志
```

**正常日志：**
- `自动同步成功`
- `新用户数据已上传到云端`
- `数据同步成功`
- `同步节流中，跳过本次同步`

**异常日志：**
- `自动同步失败: xxx`
- `上传数据失败: xxx`

### 方法2：云函数日志
```
云开发控制台 → 云函数 → 点击 syncData → 日志
```

可以看到：
- 调用时间
- 调用参数
- 返回结果
- 错误信息

### 方法3：云数据库查看
```
云开发控制台 → 数据库 → user_data → 查看记录
```

关注字段：
- `lastSyncTime`: 最后同步时间（应该持续更新）
- `updateTime`: 数据更新时间

---

## 常见问题判断

### 问题1：控制台没有同步日志

**可能原因：**
- 未登录状态（检查"我的"页面是否显示头像）
- 5秒内重复调用被节流了（这是正常的）
- syncData 云函数未部署

**解决方法：**
1. 确认已登录
2. 等待5秒后重试
3. 检查云函数部署状态

### 问题2：云数据库没有数据

**可能原因：**
- 云函数调用失败
- 数据库权限设置错误
- 网络连接问题

**解决方法：**
1. 查看云函数日志是否有错误
2. 确认 user_data 集合权限为"仅创建者可读写"
3. 检查网络连接

### 问题3：数据未及时更新

**可能原因：**
- 节流机制生效（5秒内只同步一次）
- 云函数处理中
- 浏览器缓存未刷新

**解决方法：**
1. 等待5-10秒
2. 刷新云数据库页面
3. 查看 lastSyncTime 字段

---

## 完整测试清单

测试完成后，确认以下所有项：

- [ ] 登录后自动同步（新用户上传/老用户合并）
- [ ] 修改配置自动同步（月薪、工作日、时段）
- [ ] 添加愿望自动同步
- [ ] 定时5分钟自动同步
- [ ] 页面切换触发同步（进入"我的"页面）
- [ ] 前后台切换触发同步
- [ ] 跨设备数据同步（开发工具 ↔ 真机）
- [ ] 控制台日志正常（无报错）
- [ ] 云数据库数据完整
- [ ] lastSyncTime 持续更新

---

## 性能指标参考

**正常情况下：**
- 同步响应时间：< 1秒
- 云函数调用时间：100-500ms
- 数据库写入时间：50-200ms
- 节流间隔：5秒
- 定时同步间隔：5分钟

**如果超出这些指标，检查：**
- 网络速度
- 云函数冷启动（首次调用会慢一些）
- 数据量大小

---

## 推荐测试顺序

1. ✅ **先测试登录同步**（最基础）
2. ✅ **再测试配置同步**（验证关键操作）
3. ✅ **然后测试跨设备**（最重要）
4. ✅ **最后测试定时和切换**（完整性验证）

完成以上测试后，自动同步功能就验证通过了！🎉
